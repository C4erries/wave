# Python `mbctl` demo

This example is a thin Python wrapper over `examples/bin/mbctl.exe`.

Build `mbctl` first:

```powershell
powershell -ExecutionPolicy Bypass -File .\examples\build-mbctl.ps1
```

Run:

```powershell
python .\examples\python\mbctl\wavemq_binary_demo.py
```
