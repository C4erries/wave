# MATLAB `mbctl` demo

This example is a MATLAB wrapper over `examples/bin/mbctl.exe`.

Build `mbctl` first:

```powershell
powershell -ExecutionPolicy Bypass -File .\examples\build-mbctl.ps1
```

Open and run:

```text
examples/matlab/mbctl/wavemq_binary_demo.m
```
