# MATLAB examples

MATLAB examples are split by access method:

- `examples/matlab/mbctl`
- `examples/matlab/mqtt`

Use `mbctl` if you want a wrapper over the native `wave-mq` client flow.
Use `mqtt` if you want the shortest MATLAB-native messaging demo through `mqttclient`.
