# MATLAB MQTT demo

This example uses MATLAB `mqttclient` directly and talks to the broker MQTT endpoint.

Open and run:

```text
examples/matlab/mqtt/mqtt_demo.m
```

Default broker settings inside the script:

- host: `127.0.0.1`
- port: `1883`
