# deploy

Docker Compose файлы для запуска стека.

| Файл | Описание |
|------|----------|
| `docker-compose.dev.yml` | Только broker + UI, для разработки |
| `docker-compose.demo.yml` | broker + UI + wave-gen + wave-fft |

Используйте скрипты из `../scripts/` для удобного запуска.
