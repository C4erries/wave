# orchestrator

YAML-driven pipeline orchestrator — запланировано, будет реализовано в следующем этапе.

Позволит описывать цепочки `source → operator → sink` в одном YAML-файле
и запускать их одной командой без ручного запуска wave-gen / wave-fft / wave-osc.

## Статус

Заглушка. Код отсутствует.
